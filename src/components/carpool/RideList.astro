<!-- 3-b단계: 카풀 타기 (목록에서 선택) -->
<section id="step-rider" class="mt-6" hidden>
  <button type="button" data-role-back
          class="font-round text-[16px] text-[var(--ink)]/60 px-2 py-2 active:translate-y-0.5 transition mb-1">
    ← 역할 다시
  </button>
  <p class="font-round text-[15px] text-[var(--pool)] ml-1">자리 남은 카풀을 골라 타세요! (한 개만 선택돼요)</p>
  <div id="rideList" class="mt-4 space-y-4"></div>
</section>
